name: Publish Package

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Conan
        run: |
          python -m pip install --upgrade pip
          pip install conan
      
      - name: Detect Conan profile
        run: conan profile detect --force

      - name: Cache Conan packages
        uses: actions/cache@v4
        with:
          path: ~/.conan2
          key: ${{ runner.os }}-conan-${{ hashFiles('**/conanfile.txt') }}
          restore-keys: |
            ${{ runner.os }}-conan-

      - name: Configure
        run: |
          mkdir -p build
          cd build
          conan install .. --output-folder=. --build=missing
          cmake .. \
            -DCMAKE_BUILD_TYPE=Release \
            -DCLLM_BUILD_TESTS=ON \
            -DCLLM_ENABLE_LOGGING=ON

      - name: Build
        run: |
          cd build
          cmake --build . --config Release

      - name: Test
        run: |
          cd build
          ctest --build-config Release --output-on-failure

      - name: Create Conan package
        run: |
          conan create . --version ${{ github.event.release.tag_name }} --user ${{ secrets.CONAN_USERNAME }} --channel stable

      - name: Upload to Conan Center
        run: |
          conan upload "NovaLLM/${{ github.event.release.tag_name }}@${{ secrets.CONAN_USERNAME }}/stable" --all -r conancenter
        env:
          CONAN_PASSWORD: ${{ secrets.CONAN_PASSWORD }} 