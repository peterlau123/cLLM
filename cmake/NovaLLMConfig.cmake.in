@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(fmt)
if(@NOVA_LLM_ENABLE_LOGGING@)
    find_dependency(spdlog)
endif()
if(@NOVA_LLM_BUILD_TESTS@)
    find_dependency(GTest)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake") 